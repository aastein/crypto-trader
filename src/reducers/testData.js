import * as actionType from '../actions/actionTypes';

const INIT_TEST_DATA_STATE = {
  data: [],
};

const testData = (state = INIT_TEST_DATA_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default testData;
